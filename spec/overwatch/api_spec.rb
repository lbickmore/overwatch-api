require 'spec_helper'

describe Overwatch::Api do
  it 'has a version number' do
    expect(Overwatch::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
